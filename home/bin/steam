#!/bin/sh

case $1 in
	b|blanklily ) S_HOME="blanklily"; shift ;;
	c|crazykill_97 ) S_HOME="crazykill_97"; shift  ;;
	* ) S_HOME="blanklily" ;;
esac

HOME="/home/mou/tmp/homes/steam-${S_HOME}"

[ -d ${HOME} ] || mkdir -p "${HOME}"
exec /usr/bin/steam "$@"
