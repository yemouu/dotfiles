diff --git a/src/main.cc b/src/main.cc
index 301fae91..6dacbe77 100644
--- a/src/main.cc
+++ b/src/main.cc
@@ -474,7 +474,7 @@ void compileOutput( const char *argv0, const bool inSource, char *srcLocation )
 		length += strlen( *ip ) + 3;
 	for ( ArgsVector::Iter lp = libraryPaths; lp.lte(); lp++ )
 		length += strlen( *lp ) + 3;
-#define COMPILE_COMMAND_STRING "gcc -Wall -Wwrite-strings" \
+#define COMPILE_COMMAND_STRING "clang -Wall -Wwrite-strings" \
 		" -g" \
 		" -o %s" \
 		" %s"
